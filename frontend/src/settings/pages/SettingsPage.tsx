import { PageFrame } from "@/shared/ui/page-frame";

export function SettingsPage() {
  return (
    <PageFrame
      title="Settings"
      description="A stable settings shell for profile preferences, workspace controls, and system options."
    />
  );
}
